classdef (Abstract) CapturedImage
    properties
        focal_length
        shutter_type
        capture_timestamp 
    end
    %    methods
    %       function r = roundOff(obj)
    %          r = round([obj.Value],2);
    %       end
    %       function r = multiplyBy(obj,n)
    %          r = [obj.Value] * n;
    %       end
%    end
end